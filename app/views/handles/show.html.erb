<% if current_user %>
  <%= link_to "Add a public key", new_handle_key_path(handle_id: @handle.name) %>
<% end %>

<h2>Public Keys:</h2>
<% @handle.keys.verified.each do |key| %>
  🗝 <small>(minisign)</small> <%= link_to key.key_id, handle_key_path(@handle.name, key) %>
<% end %>
