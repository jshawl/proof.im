<!DOCTYPE html>
<html>
  <head>
    <title>ProofIm</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1>
      <%= link_to "proof.im", root_path %>
      /
      <%= link_to @handle.name, handle_path(@handle.name) if @handle %>
    </h1>
    <%= yield %>
  </body>
</html>
