Add a new key for <%= @handle.name %>:

<pre><code>minisign -G && cat minisign.pub</code></pre>

<%= form_with model: [:handle, @key] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit %>
<% end %>
