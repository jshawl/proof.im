Add a new key for <%= @handle.name %>:

<pre><code># Generate a new private/public key pair
minisign -G

# Get the public key's contents
cat minisign.pub

# OR

# Generate a new private/public key pair
ssh-keygen -t rsa -b 4096

# Get the public key's contents
cat ~/.ssh/id_rsa.pub
</code></pre>

<%= form_with model: [:handle, @key] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit %>
<% end %>
